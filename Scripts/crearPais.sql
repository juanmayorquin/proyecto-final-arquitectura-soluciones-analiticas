CREATE TABLE stg1_pais (
    id_area BIGINT,
    area        VARCHAR(100)
);

CREATE TABLE stg2_pais (
    id_area BIGINT,
    area VARCHAR(100)
);

CREATE TABLE pais (
    id_area BIGINT PRIMARY KEY,
    area VARCHAR(100)
);